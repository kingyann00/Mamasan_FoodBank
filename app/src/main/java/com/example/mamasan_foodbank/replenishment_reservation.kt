package com.example.mamasan_foodbank

import android.content.res.ColorStateList
import android.graphics.Color
import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.ProgressBar
import com.example.mamasan_foodbank.databinding.ActivityReplenishmentReservationBinding

class replenishment_reservation : AppCompatActivity() {
    private lateinit var  binding: ActivityReplenishmentReservationBinding
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
//        binding = ActivityReplenishmentReservationBinding.inflate(layoutInflater)
        setContentView(R.layout.activity_replenishment_reservation)
//            val bar : ProgressBar = findViewById(R.id.progressBar)
//
//        val stok_bar : ProgressBar = findViewById(R.id.stok_bar)
//
//
//        val donation_bar : ProgressBar = findViewById(R.id.donation_bar)


    }
}